diff --git a/node_modules/react-native/Libraries/Renderer/shims/ReactNative.js b/node_modules/react-native/Libraries/Renderer/shims/ReactNative.js
new file mode 100644
index 0000000..9cf3a51
--- /dev/null
+++ b/node_modules/react-native/Libraries/Renderer/shims/ReactNative.js
@@ -0,0 +1,20 @@
+/**
+ * Stub shim for React Native 0.86+ (New Architecture / Fabric).
+ *
+ * ReactNative.js was removed in RN 0.86 — the Old Architecture (Paper) renderer
+ * no longer ships. All renderer functionality moved to ReactFabric.js.
+ *
+ * This stub exists so react-native-reanimated 3.x can be bundled by Metro.
+ * The code path that requires this file (`else` branch of `isFabric()` check)
+ * is NEVER executed at runtime on New Architecture — this is dead code.
+ *
+ * Remove this file when upgrading react-native-reanimated to v4+.
+ */
+'use strict';
+
+module.exports = {
+  findHostInstance_DEPRECATED: null,
+  default: {
+    findHostInstance_DEPRECATED: null,
+  },
+};
